import * as selectors from "../../selectors.js";
import pop from "../../selectors.js";
1;
// function getRandom() {
//   return Math.floor(Math.random() * 256);
// }
// btn.addEventListener("click", function () {
//   let target = selectors.p1;
//   let targetSepareted = target.textContent.split('').map(elem=> `<span style="color: rgb(${getRandom()}, ${getRandom()}, ${getRandom()})">${elem}</span>`)
//   target.innerHTML = ''
//   targetSepareted.forEach(elem=> target.insertAdjacentHTML('beforeend',elem))
// });
2;
// selectors.btn.addEventListener("click", function () {
//   let data = selectors.p1.innerText.toLowerCase()
//   .trim()
//   .split(" ");
//   let value = selectors.inp1.value.trim().toLowerCase()
//   let targetIndex = data.indexOf(value)
//   console.log(targetIndex)
//   if (targetIndex === -1) return
//   data[targetIndex] = `<span style="color: red">${data[targetIndex]}</span>`
//   selectors.p1.innerHTML = ''
//   data.forEach(elem=> selectors.p1.insertAdjacentHTML('beforeend', elem + ' '))
// });
3;
selectors.btn.addEventListener('click', function () {
   console.log(document.URL) 
})
4;
// selectors.btn.addEventListener('click', function () {})
5;
// selectors.btn.addEventListener('click', function () {})
6;
// selectors.btn.addEventListener('click', function () {})
